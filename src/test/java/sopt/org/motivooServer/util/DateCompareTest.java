package sopt.org.motivooServer.util;

import static org.junit.jupiter.api.Assertions.*;

import java.time.LocalDate;
import java.time.LocalDateTime;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import lombok.extern.slf4j.Slf4j;

@Slf4j
public class DateCompareTest {

	/*@DisplayName("오늘의 미션 조회를 위한 날짜 비교 테스트")
	@Test
	void compareToTodayDate() {
		LocalDate date = LocalDate.of(2024, 8, 17);
		LocalDate date2 = LocalDate.of(2024, 1, 11);
		LocalDate today = LocalDate.now();

		assertEquals(date2, today);
	}

	@DisplayName("자정마다 어제 날짜 구하기")
	@Test
	void getYesterdayDate() {
		LocalDateTime yesterday = LocalDateTime.now().minusDays(1);
		log.info("어제 날짜: {}", yesterday);
	}

	@DisplayName("LocalDateTime -> LocalDate로 변환하여 비교 테스트")
	@Test
	void getLocalDateCompare() {
		LocalDateTime now = LocalDateTime.now().minusDays(1);
		LocalDate target = LocalDate.of(2024, 1, 10);

		assertEquals(target.isEqual(now.toLocalDate()), true);
	}*/
}
